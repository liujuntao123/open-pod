# Open Pod

多人有声书 / 广播剧生产上下文：把分角色剧本合成为成品有声内容。

## Language

**有声书作品 (Audiobook Work)**：
一次完整的多人有声内容生产对象，由章节、作品角色与配音设定组成，最终产出可听的成品音频。整部作品绑定唯一 TTS Provider，决定该作品工作台的音色选择、参数模式与生成栈。
_Avoid_: 播客节目, Podcast, Episode（除非后续明确支持栏目化发布）; 章级或多 Provider 混绑的单一作品

**广播剧 (Audio Drama)**：
有声书作品的一种形态，强调多角色对白、场景与表演感；与有声书作品共用同一生产模型。
_Avoid_: 把「广播剧」和「有声书」拆成两套互不兼容的产品对象

**音色 (Voice)**：
可配置的 TTS 发音配置（模型、说话人、风格与默认可调参数）；是跨作品的 TTS 资产，且归属单一 TTS Provider 种类。
_Avoid_: 角色（角色是剧情主体；音色是发声配置）; 跨 Provider 的「同一音色」抽象

**音色库 (Voice Library)**：
本地工作室内可复用的音色集合；音色不属于某一有声书作品。展示与选用按 TTS Provider 种类分区；作品角色只能绑定与该作品 Provider 种类一致的音色。
_Avoid_: 把音色做成仅作品内私有且不可复用的唯一形态; 在作品工作台混列全部 Provider 的音色而不过滤

**作品角色 (Work Character)**：
归属某一部有声书作品的叙事说话主体；引用音色库中与作品 TTS Provider 种类一致的音色，并可在角色级覆盖默认朗读参数。剧本中的「角色」均指作品角色。
_Avoid_: 全局角色库（v1 不把叙事角色做成跨作品身份）; 用户账号; Speaker（除非指 TTS 引擎侧的发音人）; 角色绑定与作品 Provider 种类不符的音色

**本地工作室 (Local Studio)**：
单用户、单实例的有声书生产环境；作品、音色配置与生成/合成任务都属于该实例，v1 不做多租户与账号体系。
_Avoid_: 平台租户, Workspace（除非后续引入协作）, SaaS 账户

**工作室数据目录 (Studio Data Directory)**：
本地工作室的权威数据根；存放作品与剧本元数据、音色库、Provider 连接与密钥、生成与合成任务状态，以及行音频/章合成音频文件。浏览器不是主存储。
_Avoid_: localStorage 作为作品主库；把密钥写进章合成音频包或前端打包产物

**密钥存储 (Secrets Store)**：
工作室数据目录内与作品内容分离的连接鉴权材料存放处；仅本地后端读取，不进入章合成产物。
_Avoid_: 把 API Key 嵌进音色展示名或剧本 JSON 明文共享字段（实现上可同目录但须分文件/分表并收紧权限）

**工作室数据库 (Studio Database)**：
工作室数据目录内的 SQLite 库，承载有声书作品、章节、结构化剧本（台词行）、作品角色、音色库、Provider 连接元数据、生成/合成任务与行音频槽的索引/指纹等权威元数据。
_Avoid_: 以浏览器存储或散落多份手改 JSON 作为主元数据源

**音频文件存储 (Audio File Store)**：
工作室数据目录内存放行音频与章合成成品等二进制文件的区域；数据库只保存路径与指纹等引用，不把音频 blob 作为主存。
_Avoid_: 把全部音频塞进 SQLite blob 作为 v1 默认

*Implementation stack choices are recorded in ADRs, not as domain language.*

*Package layout (`packages/shared`, `apps/server`, `apps/web`) is recorded in ADR-0019, not as domain language.*

*Local HTTP binds to loopback by default (ADR-0022); not domain language.*

*MVP first vertical slice uses Fish Audio first (ADR-0023); not domain language.*

*Scope convergence (MVP vs v1 vs non-goals) is ADR-0038, not domain language.*

*Default TTS concurrency is configurable (default 1) and studio data dir defaults to `~/.open-pod` with override (ADR-0041); not domain language.*

## Surfaces

**作品列表 (Work List)**：
本地工作室中有声书作品的入口清单；用于创建、打开、删除作品。
_Avoid_: 多租户仪表盘、栏目/订阅管理

**作品工作台 (Work Bench)**：
针对单部有声书作品的主生产界面：章节导航、结构化剧本编辑、作品角色与参数、预听、生成与合成触发、任务进度。Fish 与 MiMo **共用同一套工作台流程**（含剧本生成、文件上传/批量上传、进入结构化生产、行生成与章合成）；仅音色选择、角色参数表单与演播稿方言随作品 TTS Provider 种类切换。底层共用有声书作品、章节、台词行、行音频、章合成与任务模型。
_Avoid_: 把音色库全局管理塞进工作台作为唯一设置入口; 为 MiMo 另做丢掉上传/原流程的第二套整页工作台作为主路径; UI 展示键与适配器/引擎请求键脱节

**工作室设置 (Studio Settings)**：
跨作品的工作室级配置界面：各 TTS Provider 的 API Key（及同类连接态）、任务并发、数据目录等。v1 对 Fish 与 MiMo 各维持一条内置 Provider 连接，设置中分节配置 Key，不提供多连接增删或自定义端点管理。
_Avoid_: 账号中心、计费、团队权限; 自定义多连接 CRUD 作为 v1 主路径

**自动保存 (Autosave)**：
作品工作台对结构化剧本、作品角色与参数覆盖等元数据的防抖持久化；写入工作室数据库，不触发生成。行音频槽与任务状态由生成成功路径直接写入，不经自动保存。
_Avoid_: 显式保存作为 v1 主路径；按键即生成；把音频 blob 塞进自动保存请求

**级联删除 (Cascade Delete)**：
删除章节或有声书作品时，同步删除其下属结构化剧本元数据与对应行音频/章合成音频文件。v1 无回收站。
_Avoid_: 只删元数据留下孤儿音频当作正常状态

**引用阻止删除 (Referential Block)**：
作品角色、音色、Provider 连接在仍被下游引用时禁止删除：有台词行引用的作品角色不可删；有作品角色引用的音色不可删；有音色使用的 Provider 连接不可删。须先解除引用。
_Avoid_: 级联清空引用并静默删除；删除后留下悬空引用

**工作室引导 (Studio Onboarding)**：
本地工作室的软性空状态指引：提示配置 Provider 连接、创建音色、新建有声书作品。不阻塞进入作品列表与剧本编辑；仅在生成等前置条件不满足时禁用对应动作并指向设置。
_Avoid_: 强制设置向导墙；云端示例项目；账号注册流程

**错误呈现 (Error Presentation)**：
按层处理问题：前置不满足则禁用并说明；任务失败记入任务状态与行侧标记；自动保存失败用持久提示直至恢复；合成时全章无音频硬拦截；过期音频需确认警告。v1 不以自动重试或系统通知代替可操作反馈。
_Avoid_: 所有错误统一 toast；失败自动重试；全局复杂错误码体系作为 v1 必选项

## Production

**剧本 (Script)**：
有声书作品的文本源，按说话主体组织对白与旁白；是生成与合成的唯一真相来源。
_Avoid_: 时间线工程文件, DAW 工程（时间线不是 v1 的主编辑模型）

**章节 (Chapter)**：
有声书作品内可独立编辑、生成行音频并合成为章成品的叙事单元。不绑定 TTS Provider；沿用所属作品的 Provider。
_Avoid_: Episode, 集（除非明确做连载栏目）; 章级 Provider 绑定

**台词行 (Line)**：
剧本中一次连续发言的最小生成单元，归属一个作品角色，可带局部朗读参数。
_Avoid_: 段落（未绑定角色的纯文本块）, Clip（避免过早滑向时间线语义）

**旁白 (Narrator)**：
仅作为常见角色显示名（默认「旁白」）的约定，不是单独对象类型，也不具备不可删除等特殊规则；与其它作品角色同一套绑定音色、参数覆盖与删除约束。
_Avoid_: 系统语音, 默认 Speaker, 旁白专用字段/权限

**结构化剧本 (Structured Script)**：
以稳定的台词行为单位的剧本表示；每行绑定作品角色与可选局部参数，是编辑与生成的权威形态。
_Avoid_: 把原始标记文本当作运行时主模型

**剧本导入 (Script Import)**：
把标记文本或纯文本解析为结构化剧本的一次性转换过程；导入后以结构化结果为准，不与源文本双向同步。v1 标记约定为「角色名」+ 固定全角冒号 `：` +「台词」；只认该冒号，不认半角 `:`。角色名按作品角色显示名匹配，未知则自动创建未绑音色的作品角色；`narrator`（大小写不敏感）归一为显示名「旁白」；空行丢弃；无此前缀的行整行归入名为「旁白」的普通角色。导入格式与 TTS Provider 种类无关。
_Avoid_: 实时双向绑定源文件；以正则不断重解析整章作为主编辑；同时兼容多种冒号方言；把旁白做成独立类型

**演播稿方言 (Script Performance Dialect)**：
剧本 LLM 或手写演播稿中，允许嵌入的、面向特定 TTS Provider 的表演标记习惯。随有声书作品的 TTS Provider 种类切换：Fish 作品沿用 Fish S2 方括号标签习惯；**MiMo 作品由剧本 LLM 在台词正文中生成 MiMo 风格/音频标签**（如 `(开心)`、`[叹气]`），角色级表演另由 **风格指令** 参数（可 LLM 生成）承担，不把 Fish 方括号提示套用到 MiMo。结构化「角色名：台词」排版与文件上传/导入流程本身不是方言。
_Avoid_: 全工作室单一 Fish 提示词生成 MiMo 作品稿；把演播稿方言做成与作品 Provider 无关的第三套全局规范；MiMo 只生成干净口语而禁止标签；为 MiMo 另造 JSON 富演播稿包主路径而丢掉原有纯文本剧本生成

**行选择 (Line Selection)**：
作品工作台中台词行的焦点与多选状态。当前行是焦点行；选中集合支持多行。「生成当前行」作用于焦点；「生成选中行」作用于选中集合，集合为空时退化为当前行。
_Avoid_: 把焦点与多选混成单一状态导致批量动作歧义

**行重排 (Line Reorder)**：
改变章节内台词行顺序。只影响章合成时的拼接顺序，不改变行音频指纹；已有行音频不因重排而过期。
_Avoid_: 把章节序号编入音频指纹；v1 做合并/拆分行作为必选结构操作

## TTS

**TTS Provider**：
一种可插拔的语音合成后端适配；负责把统一的合成请求翻译成该引擎的调用，并把引擎响应归一成工作室可用的音频结果。
_Avoid_: 把某个具体引擎 API 当成领域模型本身

**在线 TTS API (Online TTS API)**：
以网络 HTTP(S) 调用的外部语音合成服务；v1 的 TTS Provider 适配目标是在线 API，而非在本机加载/部署模型权重。
_Avoid_: 把本地 GPU 推理进程当作 v1 必选运行时

**Provider 连接 (Provider Connection)**：
调用某在线 TTS API 所需的连接级配置（端点、鉴权等），与单个音色配置分离。v1 每种 TTS Provider 种类一条工作室内置连接，用户只配置对应 API Key；连接可被同种类的多个音色与多部作品复用。
_Avoid_: 在每个音色里复制一份完整密钥与端点; v1 暴露多连接/自定义 base URL 管理

**作品 TTS Provider 绑定 (Work TTS Provider Binding)**：
有声书作品对唯一 TTS Provider **种类**（`fish` / `mimo`）的归属，不是对某条 Provider 连接行的引用。连接与密钥仍在工作室级按种类各一条内置连接配置；作品只决定用哪一种类的栈。作品角色所选音色必须属于该种类。新建作品默认种类为 `fish`，创建时可选 `mimo`；存量作品迁移一律回填为 `fish`。
_Avoid_: 章级 Provider 绑定; 作品内混用多个 TTS Provider; 作品绑定 `provider_connection_id`; 仅靠角色碰巧绑了哪条音色来隐含作品栈且无作品级权威; 允许作品长期处于未绑定 Provider 的空态

**作品 Provider 换绑 (Work Provider Rebind)**：
在作品工作台（非工作室设置）更改有声书作品的 TTS Provider 种类。须经明确确认后执行：解绑所有音色种类与新种类不符的作品角色音色，清空相关角色与台词行上的参数覆盖，并把受影响行标为过期行音频；不自动清空行音频槽，不自动入队重生成。创建后允许换绑，不是只读徽章。换绑后仍在同一作品工作台壳内切换音色/参数子面与演播稿方言，不另开产品入口。
_Avoid_: 静默保留错配音色; 换绑即批量重生成; 换绑后保留对新参数模式无效的覆盖键当作仍生效; 仅能通过另建作品换栈且无工作台入口

**MiMo TTS Provider**：
对接 Xiaomi MiMo 在线语音合成 API（`POST …/v1/chat/completions`，鉴权头 `api-key`）的 TTS Provider。第一期仅适配预置音色模型 `mimo-v2.5-tts`：音色身份键为 `voice`（映射 `audio.voice`）；表演双通道为 `style_instruction`（→ 可选 `role:user`）与台词正文中的 **MiMo 音频标签**（→ `role:assistant`）；非流式请求固定 `audio.format=wav` 并解码 base64 音频。音色设计（voicedesign）与音色复刻（voiceclone）不在第一期范围。
_Avoid_: 把 MiMo 的 messages/audio 整包提升为全局领域模型；第一期引入 design/clone 资产管线；UI 使用与 `audio.voice` / user 指令脱节的平行键名；把未文档化的 speed/pitch 滑杆当作 MiMo 主参数

**Fish Audio TTS Provider**：
对接 Fish Audio `POST /v1/tts` 的 TTS Provider。v1 以单说话人合成 + `reference_id`（或等价音色定位）为主路径；其多说话人对话 API 不作为本工作室主生成路径（多角色由作品角色×行级生成表达）。
_Avoid_: 把 Fish 的 multi-speaker 文本标签当作结构化剧本格式

**音色配置 (Voice Config)**：
某音色在其 TTS Provider 下的定位信息与默认参数（模型/说话人/风格等）；参数形态由该 Provider 的参数模式描述，而非全局固定字段表。
_Avoid_: 全产品写死同一套 speed/pitch 字段并假定所有引擎都认识

**音色试听 (Voice Test)**：
在音色库中对某音色配置发起的一次性试生成，使用固定短文本与当前音色参数，不创建台词行、不写入行音频槽。用于校验 Provider 连接与 reference_id 等定位是否可用；失败不阻止保存音色配置。
_Avoid_: 把音色试听做成必须门禁；用正式作品行承担连接调试

**参数模式 (Parameter Schema)**：
某 TTS Provider 声明的可调参数结构（名称、类型、范围、默认值与展示提示），用于配置 UI 与校验。
_Avoid_: 无 schema 的自由 JSON 作为唯一配置方式（可作高级出口，不作主 UX）

**生成请求 (Generate Request)**：
针对一行或多行台词的统一生成意图：文本、作品角色→音色解析结果、生效中的参数覆盖；由对应 TTS Provider 适配器执行。
_Avoid_: 在业务层直接拼各引擎私有 HTTP body

**参数覆盖 (Parameter Override)**：
在更具体层级改写音色默认参数的局部值；生效参数由音色默认与更高优先级覆盖合并而成。
_Avoid_: 每行复制一整份引擎私有请求体作为唯一存储

**生效参数 (Effective Params)**：
对某次生成请求最终采用的参数集合。合并顺序为：音色默认 ← 作品角色参数覆盖 ← 台词行参数覆盖；后者覆盖同名键。只合并参数模式中声明的键；未出现的键视为继承下层。
_Avoid_: 无序深合并、在业务层保留多份完整引擎请求体互相覆盖

**参数层 (Param Layer)**：
参数覆盖所在的层级，仅三种：音色默认、作品角色、台词行。不存在「章节默认参数」或「作品全局朗读参数」作为 v1 层（若以后需要再显式引入）。
_Avoid_: 隐式第四层、UI 状态冒充持久层

**风格指令 (Style Instruction)**：
MiMo 表演双通道中的 **user 通道**：参数键 `style_instruction`（自然语言表演控制）；映射为生成请求中可选的 `role: user` 消息 `content`，不进入合成语音文本。主要在**作品角色**上配置，可用剧本 LLM 按预置模板生成后再手改；也可出现在音色默认或台词行覆盖中，按生效参数合并。不是跨 Provider 的统一情绪枚举。
_Avoid_: 全局 emotion 枚举强行映射到所有引擎；把风格指令写死进台词正文作为唯一控制方式；与正文音频标签混成单一字段导致无法分别映射 user/assistant

**MiMo 音频标签 (MiMo Audio Tags)**：
MiMo 表演双通道中的 **assistant 通道**：嵌入台词正文的风格与细粒度控制标记，含句首整体风格（如 `(开心 慵懒)`、`(唱歌)`）与句中控制（如 `[叹气]`、`[轻笑]`）。落在结构化剧本的台词文本内，随行文本进入指纹与 `role:assistant` content；不是独立参数模式键。**剧本生成（MiMo 方言）应主动产出这些标签**；用户亦可手写。
_Avoid_: 为标签再做与正文脱节的第二套权威存储；把 Fish S2 方括号习惯与 MiMo 标签混为同一方言；把标签从正文剥离后生成时再注入却不进指纹

## Generation & Composition

产品中文用语约定（UI 与领域叙述优先）：

- **生成**：对台词行调用 TTS，产出**行音频**。
- **合成**：把多段行音频按剧本顺序拼接，并按策略可选烧录背景音乐，得到**章合成音频**（章成品）。
- **下载**：把已合成的章成品 WAV 保存到用户本机；不再次生成、不再次拼接。
- **预听**：播放单行行音频。
- **试听**：播放章合成音频。

代码/API 中的 `synthesize` / `chapter_export` 等标识符可保留英文历史名，但用户可见文案必须遵守上表。

**行音频 (Line Audio)**：
单条台词行一次成功**生成**后的音频产物；缓存与后续合成的基本单元。
_Avoid_: 把整章原始引擎响应当作唯一可编辑音频原子

**章合成音频 / 章成品 (Chapter Audio)**：
将章节内行音频按剧本顺序拼接（含行间间隙策略），并按章合成策略可选与背景音乐烧录混音后得到的单轨章节成品音频。
_Avoid_: 绕过行音频、把整章一次丢给引擎作为唯一生产路径；分轨/可分离的多轨章成品作为主交付

**任务 (Job)**：
一次可追踪的异步工作单元。v1 有两类：行生成任务（产出行音频）、章合成任务（依赖行音频后拼接成品）。批量操作是多个行生成任务的集合，不是第三种领域对象。
_Avoid_: 用一个模糊状态覆盖生成与合成两种粒度

**生成前置条件 (Generate Preconditions)**：
行可入队前必须满足：台词非空、作品角色已绑定音色、该音色的 TTS Provider 种类与作品 TTS Provider 绑定一致、该种类的 Provider 连接（API Key）可用。作品种类是生成栈权威：不得因音色碰巧属于另一 Provider 而改走其它适配器。不满足则不创建任务（禁用/提示），而不是先入队再失败。空文本行在批量中被跳过且不建任务。
_Avoid_: 未绑音色仍入队等失败；空行建 failed 任务刷屏；以音色种类覆盖作品种类导致同作品混栈

**行间间隙 (Line Gap)**：
章合成拼接时插入在相邻行音频之间的静音间隔。v1 使用工作室全局固定默认值 300ms；不属于 TTS Provider 参数，也不作为参数层的第四层。
_Avoid_: 把间隙写进每行引擎请求当作唯一控制方式；v1 做复杂的逐行间隙编辑器

**生成动作 (Generate Action)**：
用户显式触发的行级生成入队操作。v1 提供「生成当前」与「生成选中」；整章与「仅缺失/过期」是同一入队规则上的过滤快捷方式，不是另一类任务模型。
_Avoid_: 编辑即生成；用同步 HTTP 代替入队

**生成前置条件 (Generate Preconditions)**：
行可入队前必须满足：台词非空、作品角色已绑定音色、该音色的 Provider 连接可用。不满足则不创建任务（禁用/提示），而不是先入队再失败。空文本行在批量中被跳过且不建任务。
_Avoid_: 未绑音色仍入队等失败；空行建 failed 任务刷屏

**换绑过期 (Rebind Stale)**：
更改作品角色所绑音色，或更改音色定位/默认参数导致生效参数与已有行音频指纹不一致时，仅将受影响行标为过期行音频；不自动入队生成，不批量清空行音频槽。
_Avoid_: 换绑即自动重生成；换绑默认清空该角色全部音频

**生成快照 (Generate Snapshot)**：
行生成任务入队时固化的文本、音色定位与生效参数。worker 只按快照调用 Provider 并计算写入槽位的音频指纹；入队前须先刷新未落盘的自动保存，使快照与用户所见一致。任务进行中仍允许编辑该行；若完成后当前编辑态已偏离快照，则槽位相对当前为过期。
_Avoid_: 用未保存的脏状态与库不一致入队；running 时锁死编辑；中途一改就取消任务

**任务取消 (Job Cancellation)**：
用户中止尚未完成的生成或合成任务。queued 可立即标为 cancelled；running 应尽力中止上游请求，标为 cancelled，且不得写入行音频槽。cancelled 与 failed 区分：前者是用户中止，后者是执行错误。取消不删除已有行音频。
_Avoid_: 取消后仍用晚到的 Provider 响应覆盖槽位；把取消记成失败

**任务去重 (Job Dedup)**：
同一台词行若已有 queued 或 running 的行生成任务，则忽略新的同行动作，不重复入队。
_Avoid_: 连点造成同一行多任务并发写槽

**任务恢复 (Job Recovery)**：
工作室进程启动时对任务队列的处置：遗留的 running 任务标为 failed（进程中断），不自动按原快照重试；queued 任务保留并由 worker 继续消费。
_Avoid_: 启动时自动重放 running 导致重复计费；清空全部 queued

**音频指纹 (Audio Fingerprint)**：
判定某行已有行音频是否仍匹配当前生成意图的规范摘要，至少包含：规范化台词文本、音色定位身份（含 Provider/模型/说话人等）、生效参数的规范序列化、以及适配器/结果格式版本。不含行在章节中的顺序位置。
_Avoid_: 仅用行 id 判断是否过期；把章节序号编入指纹

**行音频槽 (Line Audio Slot)**：
每条台词行至多保留一份行音频产物。仅在用户显式触发生成时写入；成功则覆盖该槽位。编辑文本/音色/参数不会自动生成，也不自动清空槽位。
_Avoid_: 多版本历史栈；编辑即静默重生成；编辑即删音频

**行音频格式 (Line Audio Format)**：
v1 行音频槽与章合成成品统一使用 WAV 容器，便于拼接与插入行间间隙；不把 Provider 原始返回格式当作跨引擎统一存储格式。
_Avoid_: 按引擎各自格式混存导致拼接路径分叉；以 mp3 作为 v1 主存储

**合成交付 (Composition Delivery)**：
章合成成功后，成品 WAV 写入工作室数据目录的导出区，并可通过 UI **试听**与**下载**到本机。v1 对同一章再次合成覆盖该章成品文件，不保留多版本历史；过期警告呈现在 UI，不靠文件名标记。
_Avoid_: 只走浏览器下载却不落工作室目录；合成版本树；把「下载」与「合成」混成同一动作

**行预听 (Line Preview)**：
播放某台词行当前行音频槽中的 WAV。无槽位则不可播；过期仍可播但须标过期。生成进行中仍播旧槽（若有）。该行新生成成功时，若正在预听该行则中断并切换到新音频。
_Avoid_: 以系统外部播放器作为 v1 唯一预听方式；无音频时用错误弹窗代替禁用

**过期行音频 (Stale Line Audio)**：
槽位中仍有行音频，但其记录的音频指纹与按当前剧本/参数算出的指纹不一致。预听与章合成仍可使用该音频，但必须标为过期，提示用户可手动重新生成。
_Avoid_: 静默把旧音频冒充为当前参数结果；把过期等同于无音频

## Composition

**章合成 (Chapter Composition)**：
将一章的当前行音频（含过期但仍占用槽位的）按序拼接，并按策略可选烧录背景音乐，写出为单个成品音频文件的过程；合成前应用合成就绪规则。用户文案称「合成」，不是「导出」；「下载」仅指把已合成文件保存到本机。
_Avoid_: 合成多版本历史；以工程包或分轨包作为 v1 主交付；把行级 TTS 叫作合成

**合成就绪 (Composition Readiness)**：
章合成前对行音频槽的检查结论。至少有一行可播放音频才可合成；无音频行默认跳过并在 UI 明确提示，不整章硬拦。仅有过期行音频的台词行可合成但必须警告。全章无任何行音频时硬拦截。不把过期等同于缺失。背景音乐设定不改变上述行音频门禁；曲目不可用走预置曲目可用性，不与「缺行」混用。
_Avoid_: 静默跳过无音频行且不告知用户；把过期行强制当作硬错误与缺失同等拦截（除非用户显式选择更严策略）；因未选 BGM 或章成品过期而禁止合成

**书合成 (Work Composition)**：
按章节顺序合成多章成品音频（每章一文件，或再拼成整书长音频——具体文件布局在实现时二选一或可选），仍是整轨音频交付，不是工程包。整书拼接若发生，只串接已烧录的章合成音频，不再次选择或混入书级背景音乐。
_Avoid_: 播客 RSS/上架分发作为 v1 范围；书级背景音乐设定或整书二次烧录 BGM

**背景音乐烧录 (BGM Burn-in)**：
章合成时把所选背景音乐与人声轨混为同一条章合成音频的过程；成品中不可再分离背景音乐轨。有背景音乐时，成品开头先独奏背景音乐（章级可配置前奏秒数，默认 3 秒）再进入人声，之后背景音乐继续铺在人声下。
_Avoid_: 把背景音乐做成可单独下载的分轨；迷你 DAW 多轨工程

**背景音乐 (Background Music / BGM)**：
项目预置、可在章合成时烧录的伴奏音频资产；用户只选择，不管理其源文件或导入路径。
_Avoid_: 用户自管音频库；把 BGM 当成音色库同类「可上传资产」

**章背景音乐设定 (Chapter BGM Setting)**：
某章节记住的背景音乐选择（含「无」）、章背景音乐音量与背景音乐前奏秒数；章合成默认采用该设定，不是作品级默认，也不是仅当次弹窗的瞬时选择。新建章节时默认「无」背景音乐、音量 45、前奏 3 秒，不从同作品其他章或「上次选用」隐式继承。
_Avoid_: 作品默认 + 章节覆盖作为本阶段必选；仅合成当次选择且不落库；新章默认某首预置曲或隐式继承他章

**工作室预置曲目 (Studio Preset Track)**：
随本地工作室应用分发的只读背景音乐条目（稳定 id、展示名与内置音频）；跨作品共享。权威清单内置于应用，经只读 API 列出；用户不上传、不删除、不改源文件，也不靠数据目录热更新或远程拉取。
_Avoid_: 工作室数据目录可写资产库；作品内嵌 BGM 拷贝；与音色库混成同一类「用户资产」；远程曲目商店；启动时热插拔用户曲库

**背景音乐时长对齐 (BGM Length Alignment)**：
烧录时背景音乐循环铺满「前奏 + 整段人声轨」时长，超出部分截断；前奏为章背景音乐设定中的独奏秒数（默认 3，范围 0–30）。本阶段不做单次播放留白、也不把淡入淡出做成章设定。
_Avoid_: 只播一遍后静音；复杂包络/淡变编辑器

**章背景音乐音量 (Chapter BGM Volume)**：
章背景音乐设定中的单一相对响度控制，用户标度为 0–100 的相对滑杆，仅作用于烧录时的背景音乐增益；人声轨不做用户向音量旋钮。100 表示预设标准伴奏响度；0 表示选了曲但伴奏增益为 0，**不等于**背景音乐选择「无」（「无」不进入烧录路径）。未选曲目时该值不参与合成。
_Avoid_: 工作室仅固定全局比例且用户不可调；作品级音量层；人声/BGM 分轨混音台；自动闪避（ducking）作为本阶段必选；用音量 0 代替「无」；dB 专业标度作为主 UX

**背景音乐前奏 (BGM Intro)**：
章合成烧录时，人声进入前仅播放背景音乐的时长，由章背景音乐设定中的秒数控制（默认 3 秒，0 表示无前奏）。未选曲目时不生效。
_Avoid_: 工作室全局写死不可改；把前奏做成作品级设定作为本阶段必选

**合成快照 (Composition Snapshot)**：
章合成任务入队时固化的装配意图，至少包含参与拼接的行音频集合策略结果，以及当时的章背景音乐设定（预置曲目 id 或「无」、章背景音乐音量、背景音乐前奏秒数）。worker 只按该快照烧录；入队后改章设定不影响已在跑的任务。
_Avoid_: 合成进行中读取可变的「当前章设定」导致不可复现；把行生成快照与合成快照混成同一概念而不区分

**过期章合成音频 (Stale Chapter Audio)**：
章成品文件仍在，但其记录的合成快照中的背景音乐设定与当前章背景音乐设定不一致。试听与下载仍可用旧成品，但必须提示需重新合成；不自动重合成，不删除文件。
_Avoid_: 静默把旧成品冒充为当前 BGM 设定结果；改设定即自动重合成；改设定即删除章成品

**章背景音乐控件 (Chapter BGM Controls)**：
作品工作台中与合成本章/试听/下载相邻的章节级控件：选择工作室预置曲目（或「无」）、调节章背景音乐音量，并可试听预置曲目原曲；变更写入章背景音乐设定，不单独弹层、也不放进全局工作室设置。
_Avoid_: 仅在点合成时才出现的瞬时选择；把 BGM 放进工作室设置当作用户资产管理

**预置曲目可用性 (Preset Track Availability)**：
章背景音乐设定引用的工作室预置曲目必须在清单中存在且内置音频可读。合成入队前校验不通过则禁止入队并提示修改设定；worker 执行时仍不可用则章合成任务失败，不静默降级为无人声-only 成功成品。
_Avoid_: 找不到曲目仍标 succeeded 并只出人声；把缺失曲目当成「无」而不告知

**预置曲目试听 (Preset Track Preview)**：
播放某工作室预置曲目的内置原曲，用于挑选伴奏；可按当前章背景音乐音量调节听感，但不是与人声烧录后的混音预览。
_Avoid_: 把预置曲目试听等同于章成品试听；未合成的人声+BGM 实时叠播

**章成品试听 (Chapter Audio Playback)**：
播放当前章合成音频文件。要听到「人声 + 背景音乐烧录」的最终效果必须先完成章合成；预置曲目试听不能替代章成品试听。
_Avoid_: 与烧录结果不一致的实时叠播当作主听感；临时混音预览文件作为第三种成品

# Voices are global assets; characters are work-scoped

TTS voices live in a studio-level voice library and may be reused across audiobook works. Characters belong to a single work (narrative identity) and reference a voice, with optional character-level parameter overrides. v1 does not maintain a global character cast library.

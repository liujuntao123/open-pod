# v1 export is full-track chapter/work audio only

Primary deliverable is assembled full-track audio (per chapter, and optionally whole-work concatenation). Per-line stems, subtitle/timestamp packages, and full project bundles are not v1 export goals. Stale line audio may still be included in assembly; export does not invent multi-version history.

# Script is the source of truth; timeline is not the v1 editor

Production is script-driven: users author or paste role-tagged text, bind voices to characters, tune parameters, synthesize, and export. A DAW-style timeline is not the primary editing surface in v1 — at most a playback/preview of synthesis results. Batch-only pipelines without a script editor are also out of scope for the core UX.

# Structured lines are the editor truth; marked text is import-only

The script editor is line-structured (stable line identities, character binding, per-line overrides). Marked plain text (e.g. `角色：台词`) is supported as an import path into that structure, not as the primary editing model and not as a live two-way sync with the structured script.
